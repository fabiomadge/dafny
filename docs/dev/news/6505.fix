Parse the `--cores` percentage with the invariant culture, so a fractional percentage such as `--cores:50.5%` means the same on every machine. A percentage that is zero, negative, or too large to denote a usable number of cores is now rejected instead of silently meaning one core.
